{
	"scripts": {
		"start": "cd front-end && yarn start",
		"add": "cd front-end && yarn add",
		"install": "cd front-end && npm install"
	},
	"dependencies": {
		"use-context": "^0.0.1",
		"use-reducer": "^0.0.1"
	}
}
